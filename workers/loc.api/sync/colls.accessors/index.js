'use strict'

const PUBLIC_COLLS_CONF_NAMES = require('./public.colls.conf.names')
const PublicCollsConfAccessors = require(
  './public.colls.conf.accessors'
)

module.exports = {
  PUBLIC_COLLS_CONF_NAMES,
  PublicCollsConfAccessors
}
