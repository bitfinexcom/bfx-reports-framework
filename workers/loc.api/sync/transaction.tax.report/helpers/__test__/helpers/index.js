'use strict'

const {
  mockTradesForNextYear,
  mockTrades
} = require('./mock-trades')
const getMockedTrades = require('./get-mocked-trades')

module.exports = {
  mockTradesForNextYear,
  mockTrades,
  getMockedTrades
}
